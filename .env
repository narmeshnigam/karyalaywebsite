# SellerPortal System - Production Environment Configuration
# IMPORTANT: Copy this file to .env on your Hostinger server and update the values
# File permissions should be 600 (owner read/write only)

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Environment: MUST be 'production' for live server
APP_ENV=production

# Debug mode: MUST be 'false' for production (prevents error display to users)
APP_DEBUG=false

# Application URL: Your actual domain (with https)
APP_URL=https://karyalay.in

# =============================================================================
# Unix socket: Leave empty for Hostinger (uses TCP/IP)

# Enable persistent connections for better performance

# =============================================================================
# PAYMENT GATEWAY (Razorpay)
# =============================================================================
# Get these from your Razorpay Dashboard > Settings > API Keys

RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx
RAZORPAY_WEBHOOK_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# ADMIN SETTINGS
# =============================================================================

ADMIN_EMAIL=admin@karyalay.in

# =============================================================================
# DEPLOYMENT CHECKLIST
# =============================================================================
# 
# Before deploying to Hostinger:
# 
# 1. Copy this file to .env on your Hostinger server

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# Leave empty if not yet configured for production

# =============================================================================
# DUAL ENVIRONMENT DATABASE CONFIGURATION
# =============================================================================
# The system automatically detects the environment and uses appropriate credentials.
# - On localhost: Uses DB_LOCAL_* credentials
# - On production: Uses DB_LIVE_* credentials (if available, else falls back to local)
# To force localhost credentials on any environment, comment out or remove DB_LIVE_* values.

# -----------------------------------------------------------------------------
# ACTIVE DATABASE CREDENTIALS (Auto-populated based on environment)
# -----------------------------------------------------------------------------
DB_HOST=db.example.com
DB_PORT=3306
DB_NAME=testdb
DB_USER=admin
DB_PASS=
DB_UNIX_SOCKET=

# -----------------------------------------------------------------------------
# LOCALHOST CREDENTIALS (Development Environment)
# -----------------------------------------------------------------------------
DB_LOCAL_HOST=db.example.com
DB_LOCAL_PORT=3307
DB_LOCAL_NAME=testdb
DB_LOCAL_USER=root
DB_LOCAL_PASS=password123
DB_LOCAL_UNIX_SOCKET=

# -----------------------------------------------------------------------------
# LIVE CREDENTIALS (Production Environment)
# -----------------------------------------------------------------------------
# Leave empty if not yet configured for production
DB_LIVE_HOST=db.example.com
DB_LIVE_PORT=3306
DB_LIVE_NAME=testdb
DB_LIVE_USER=admin
DB_LIVE_PASS=
DB_LIVE_UNIX_SOCKET=